/**
 * Quiet vignette (signal wash bottom-left, sync wash right) and faint grain on
 * the page plate. Keyboard focus is an opaque sync outline.
 */
export function Atmosphere(_props: Record<string, never>) {
  return null
}
